<Window x:Class="Autopatch.Demo.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Autopatch.Demo.WPF.Views"
        Title="🍕 Poller's Pizza Palace - AutoPatch Demo" 
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Main Content -->
            <RowDefinition Height="Auto"/> <!-- Status Bar -->
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="DarkBlue" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🍕" FontSize="24" Foreground="White" VerticalAlignment="Center"/>
                <TextBlock Text="Poller's Pizza Palace" FontSize="20" FontWeight="Bold" 
                          Foreground="White" Margin="10,0" VerticalAlignment="Center"/>
                <TextBlock Text="AutoPatch Framework Live Demo" FontSize="14" 
                          Foreground="LightGray" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
        
        <!-- Main Content Grid -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" MinWidth="400"/> <!-- Orders -->
                <ColumnDefinition Width="1*" MinWidth="400"/> <!-- Driver Map -->
            </Grid.ColumnDefinitions>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/> <!-- Orders & Map -->
                <RowDefinition Height="1*"/> <!-- Statistics -->
            </Grid.RowDefinitions>
            
            <!-- Orders List -->
            <Border Grid.Column="0" Grid.Row="0" BorderBrush="Gray" BorderThickness="1" 
                    Margin="0,0,5,5" Padding="10">
                <views:OrdersView DataContext="{Binding OrdersViewModel}"/>
            </Border>
            
            <!-- Driver Map -->
            <Border Grid.Column="1" Grid.Row="0" BorderBrush="Gray" BorderThickness="1" 
                    Margin="5,0,0,5" Padding="10">
                <views:DriversMapView DataContext="{Binding DriversViewModel}"/>
            </Border>
            
            <!-- Statistics -->
            <Border Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" BorderBrush="Gray" 
                    BorderThickness="1" Margin="0,5,0,0" Padding="10">
                <views:StatisticsView DataContext="{Binding StatisticsViewModel}"/>
            </Border>
            
        </Grid>
        
        <!-- Status Bar -->
        <Border Grid.Row="2" Background="LightGray" Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10,0">
                    <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,5,0">
                        <Ellipse.Fill>
                            <SolidColorBrush Color="{Binding ConnectionStatusColor}"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <TextBlock Text="{Binding ConnectionStatus}" VerticalAlignment="Center"/>
                </StackPanel>
                
                <TextBlock Grid.Column="2" Text="{Binding CurrentTime, StringFormat='{}{0:HH:mm:ss}'}" 
                          VerticalAlignment="Center" FontFamily="Courier New"/>
            </Grid>
        </Border>
        
    </Grid>
</Window>
